{
  "name": "openseanfttrades",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ logicdev406/Openseanfttrades",
    "create-local": "graph create --node http://localhost:8020/ openseanfttrades",
    "remove-local": "graph remove --node http://localhost:8020/ openseanfttrades",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 openseanfttrades"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
